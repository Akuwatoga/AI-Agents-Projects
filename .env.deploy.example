# Use / for an independent domain, or /subdirectory/ behind a path-based proxy.
VITE_BASE_PATH=/
VITE_DEFAULT_LOCALE=zh-CN

# Public hostname routed by the server's Coolify Traefik proxy.
AGENT_PROJECT_DOMAIN=agent-project.zqtechlab.com

# Optional image name/tag used by Docker Compose.
ATLAS_IMAGE=ai-agents-atlas:local
